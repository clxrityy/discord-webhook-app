import { webhookUserFormInputs, embedFormInputs, webhookFormInputs } from "./constants";

const config = {
    inputs: {
        embed: embedFormInputs,
        webhook: webhookFormInputs,
        webhookUser: webhookUserFormInputs
    }
}